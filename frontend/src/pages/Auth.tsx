import {useState} from 'react'; import {supabase} from '../lib/supabase';
export function Auth(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [signup,setSignup]=useState(false);const [msg,setMsg]=useState('');const submit=async()=>{setMsg('');const r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(r.error)setMsg(r.error.message);else if(signup)setMsg('Cadastro criado. Verifique seu e-mail se a confirmação estiver ativa.');};return <div className="min-h-screen grid place-items-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7"><div className="mb-6"><div className="text-3xl font-black">MyPage<span className="gradient-text">Flow</span></div><p className="mt-1 text-white/50">Seu estúdio pessoal de vídeos em lote.</p></div><div className="space-y-3"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail" className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none"/><input value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()} placeholder="Senha" type="password" className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none"/><button onClick={submit} className="btn btn-primary w-full">{signup?'Criar conta':'Entrar'}</button>{msg&&<div className="rounded-xl bg-white/5 p-3 text-sm text-white/65">{msg}</div>}</div><button onClick={()=>setSignup(!signup)} className="mt-5 w-full text-center text-sm text-violet-300 hover:text-white">{signup?'Já tenho conta':'Criar minha conta'}</button></div></div>}
